server:
  port: 8081
spring:
  application:
    name: department-service
  datasource:
    password: vullnet2004
    url: jdbc:postgresql://localhost:5432/microservice
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true

